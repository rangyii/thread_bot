[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "thread-field-briefing-bot"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
thread-bot = "thread_bot.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
